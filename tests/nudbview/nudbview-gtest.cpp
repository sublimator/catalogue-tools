#include <gtest/gtest.h>
#include <nudbview/nudb.hpp>

// Basic smoke test to ensure nudbview headers compile
TEST(NuDBView, HeadersCompile)
{
    // Just verify that the headers compile and basic types exist
    EXPECT_TRUE(true);
}
